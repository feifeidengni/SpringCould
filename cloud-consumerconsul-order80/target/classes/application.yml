server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    #  配置 consul的相关信息
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}